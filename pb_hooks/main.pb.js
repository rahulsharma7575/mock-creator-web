// GENERATED by build_hooks.py - DO NOT EDIT DIRECTLY.
// Run: uv run python build_hooks.py > main.pb.js  (or edit the generator)
// PocketBase v0.39.10 JSVM. Handlers are fully self-contained because
// PB source-serializes handlers and re-evaluates them per request.
// Top-level declarations are NOT visible inside handlers, so all shared
// snippets are physically inlined into every handler body.

// Serve the /creator/ admin GUI (pb_public/creator). The page shell is public
// (like PB's own _/ admin) - every data endpoint behind it is superuser-only,
// and the GUI gates itself behind a superuser login screen.
routerAdd("GET", "/creator/{path...}", $apis.static($filepath.join($filepath.dir(__hooks), "pb_public", "creator"), { indexFallback: true }))

routerAdd("GET", "/api/creator/health", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  ensureCollections()
  var version = "26"
  var checkField = function (col, name) {
    try {
      var c = $app.findCollectionByNameOrId(col)
      return !!c.fields.getByName(name)
    } catch (err) { return false }
  }
  var cfgNeed = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                 "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                 "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                 "image_style_prompt", "push_enabled"]
  var jobsNeed = ["gen_type", "pdf"]
  var missing = []
  for (var i = 0; i < cfgNeed.length; i++) { if (!checkField("mock_config", cfgNeed[i])) missing.push("mock_config." + cfgNeed[i]) }
  for (var j = 0; j < jobsNeed.length; j++) { if (!checkField("mock_jobs", jobsNeed[j])) missing.push("mock_jobs." + jobsNeed[j]) }
  var pdfOk = false
  try {
    var pf = $app.findCollectionByNameOrId("mock_config").fields.getByName("pdf_parser")
    var pv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
    pdfOk = pv.indexOf("upstage") >= 0 && pv.indexOf("mistral") >= 0
  } catch (errF) {}
  if (!pdfOk) missing.push("mock_config.pdf_parser (values)")
  return e.json(200, { ok: missing.length === 0, version: version, migrations_ok: missing.length === 0, missing: missing })
} catch (err) {
  return e.json(500, { error: String(err) })
}

})

routerAdd("GET", "/api/creator/ping", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  return e.json(200, { ok: true, hooks: "loaded" })
} catch (err) {
  return e.json(500, { error: String(err) })
}

})

routerAdd("GET", "/api/creator/ensure", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  ensureCollections()
  return e.json(200, { ok: true, collections: ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"] })
} catch (err) {
  return e.json(500, { error: String(err) })
}

}, $apis.requireSuperuserAuth())

routerAdd("GET", "/api/creator/verify-model", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  var slug = (e.request.url.query().get("slug") || "").trim()
  if (!slug) return e.json(400, { error: "slug query param required" })
  if (slug.indexOf("fal-ai/") === 0) {
    var fkey = $os.getenv("FAL_KEY") || ""
    if (!fkey) return e.json(200, { exists: null, status: 0, message: "FAL_KEY is not set in the container - fal model checks unavailable" })
    var q = slug.split("/").pop().split(":")[0]
    var fres = $http.send({ url: "https://api.fal.ai/v1/models?q=" + encodeURIComponent(q) + "&limit=50", method: "GET", headers: { "Authorization": "Key " + fkey }, timeout: 30 })
    var found = false
    try { var fj = JSON.parse(fres.raw || "{}"); var list = fj.models || []; for (var i = 0; i < list.length; i++) { if (list[i].endpoint_id === slug) { found = true; break } } } catch (errF) {}
    if (fres.statusCode === 401) return e.json(200, { exists: null, status: 401, message: "FAL_KEY rejected by fal.ai (401)" })
    return e.json(200, { exists: found, status: fres.statusCode || 0, message: found ? "" : "model not found on fal.ai" })
  }
  var key = $os.getenv("OPENROUTER_API_KEY") || ""
  if (!key) return e.json(200, { exists: null, status: 0, message: "OPENROUTER_API_KEY is not set in the container - model checks unavailable" })
  var res = $http.send({
    url: "https://openrouter.ai/api/v1/chat/completions",
    method: "POST",
    headers: { "Authorization": "Bearer " + key, "Content-Type": "application/json" },
    body: JSON.stringify({ model: slug, messages: [{ role: "user", content: "ping" }], max_tokens: 1 }),
    timeout: 60
  })
  var status = res.statusCode || 0
  var msg = ""
  try { var j = JSON.parse(res.raw || "{}"); msg = (j.error && j.error.message) || "" } catch (err) {}
  var invalid = /not a valid model|does not exist|model not found|not found/i.test(msg)
  var exists = null
  if (status === 200) exists = true
  else if (status === 401) exists = null
  else if (status === 404 || invalid) exists = false
  else exists = true
  return e.json(200, { exists: exists, status: status, message: String(msg).slice(0, 200) })
} catch (err) {
  return e.json(500, { error: String(err) })
}

}, $apis.requireSuperuserAuth())

routerAdd("GET", "/api/creator/gemini-status", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  var key = $os.getenv("GEMINI_API_KEY") || ""
  if (!key) return e.json(200, { ok: false, valid: false, models: [], message: "GEMINI_API_KEY is not set in the container env" })
  var res = $http.send({ url: "https://generativelanguage.googleapis.com/v1beta/models?key=" + encodeURIComponent(key), method: "GET", timeout: 30 })
  if (res.statusCode !== 200) return e.json(200, { ok: false, valid: false, models: [], message: "Gemini key rejected (HTTP " + (res.statusCode || 0) + ")" })
  var j = {}
  try { j = JSON.parse(res.raw || "{}") } catch (err) {}
  var models = []
  var list = j.models || []
  for (var i = 0; i < list.length; i++) {
    var m = list[i]
    var methods = m.supportedGenerationMethods || []
    var okGen = false
    for (var mm = 0; mm < methods.length; mm++) { if (methods[mm] === "generateContent") okGen = true }
    if (okGen) models.push({ name: String(m.name || "").replace("models/", ""), input_limit: m.inputTokenLimit, output_limit: m.outputTokenLimit })
  }
  return e.json(200, { ok: true, valid: true, models: models, message: "" })
} catch (err) {
  return e.json(500, { error: String(err) })
}

}, $apis.requireSuperuserAuth())

routerAdd("POST", "/api/creator/push-status", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  var req = {}
  try { req = e.requestInfo().body || {} } catch (err) {}
  var base = String(req.base || "").trim().replace(/\/+$/, "")
  var email = String(req.email || "").trim()
  var pass = String(req.pass || "")
  if (!pass) { pass = $os.getenv("MOCK_PB_PASS") || "" }
  if (!base) return e.json(200, { ok: true, connected: false, total_exams: 0, url: "", mocks: [], message: "base URL missing" })
  if (!email || !pass) return e.json(200, { ok: true, connected: false, total_exams: 0, url: base, mocks: [], message: "email/password missing - set them in Push or MOCK_PB_PASS" })
  var res = $http.send({
    url: base + "/api/collections/_superusers/auth-with-password",
    method: "POST", headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ identity: email, password: pass }), timeout: 10
  })
  if (res.statusCode !== 200) {
    return e.json(200, { ok: true, connected: false, total_exams: 0, url: base, mocks: [], message: "auth failed (HTTP " + (res.statusCode || 0) + ") - check email/password" })
  }
  var tok = ""
  try { tok = JSON.parse(res.raw || "{}").token || "" } catch (err) {}
  if (!tok) return e.json(200, { ok: true, connected: false, total_exams: 0, url: base, mocks: [], message: "auth returned no token" })
  var ex = $http.send({
    url: base + "/api/collections/exams/records?perPage=200&fields=id,title,status",
    method: "GET", headers: { "Authorization": "Bearer " + tok }, timeout: 15
  })
  var mocks = [], total = 0, published = 0, draft = 0
  try {
    var exj = JSON.parse(ex.raw || "{}")
    var items = exj.items || []
    total = Number(exj.totalItems) || items.length || 0
    for (var i = 0; i < items.length; i++) {
      var it = items[i]
      var t = String(it.title || "")
      var st = String(it.status || "")
      var mm = t.match(/Mock Test ?\(?(\d+)\)?/i)
      var serial = mm ? Number(mm[1]) : -1
      if (st === "published") published++
      else if (st === "draft") draft++
      mocks.push({ title: t, status: st, serial: serial })
    }
    mocks.sort(function (a, b) { return a.serial - b.serial })
  } catch (err) {}
  return e.json(200, { ok: true, connected: true, total_exams: total, published: published, draft: draft, mocks: mocks, url: base, message: "" })
} catch (err) {
  return e.json(200, { ok: true, connected: false, total_exams: 0, url: "", mocks: [], message: "unreachable: " + String(err).slice(0, 80) })
}

}, $apis.requireSuperuserAuth())

routerAdd("GET", "/api/creator/or-status", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  var key = $os.getenv("OPENROUTER_API_KEY") || ""
  if (!key) return e.json(200, { ok: false, message: "OPENROUTER_API_KEY is not set in the container" })
  var res = $http.send({ url: "https://openrouter.ai/api/v1/credits", method: "GET", headers: { "Authorization": "Bearer " + key }, timeout: 30 })
  var j = {}
  try { j = JSON.parse(res.raw || "{}") } catch (err) {}
  var d = j.data || {}
  var total = Number(d.total_credits || 0)
  var used = Number(d.total_usage || 0)
  return e.json(200, {
    ok: res.statusCode === 200,
    total: total,
    used: used,
    remaining: Math.max(0, Math.round((total - used) * 100) / 100),
    status: res.statusCode || 0
  })
} catch (err) {
  return e.json(500, { error: String(err) })
}

}, $apis.requireSuperuserAuth())

routerAdd("GET", "/api/creator/fal-status", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  var key = $os.getenv("FAL_KEY") || ""
  if (!key) return e.json(200, { ok: false, message: "FAL_KEY is not set in the container" })
  var res = $http.send({ url: "https://api.fal.ai/v1/account/billing?expand=credits", method: "GET", headers: { "Authorization": "Key " + key }, timeout: 30 })
  var j = {}
  try { j = JSON.parse(res.raw || "{}") } catch (err) {}
  var creds = j.credits || {}
  return e.json(200, {
    ok: res.statusCode === 200,
    balance: creds.current_balance,
    currency: creds.currency || "USD",
    username: j.username || "",
    status: res.statusCode || 0
  })
} catch (err) {
  return e.json(500, { error: String(err) })
}

}, $apis.requireSuperuserAuth())

routerAdd("GET", "/api/creator/magnific-status", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  var key = $os.getenv("MAGNIFIC_API_KEY") || ""
  if (!key) return e.json(200, { ok: false, key_set: false, message: "MAGNIFIC_API_KEY is not set in the container" })
  return e.json(200, { ok: true, key_set: true, message: "key set - credit balance is not exposed via the Magnific API" })
} catch (err) {
  return e.json(500, { error: String(err) })
}

}, $apis.requireSuperuserAuth())

routerAdd("GET", "/api/creator/upstage-status", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  var key = $os.getenv("UPSTAGE_API_KEY") || ""
  if (!key) return e.json(200, { ok: false, valid: false, message: "UPSTAGE_API_KEY not set (optional - vision OCR is used instead)" })
  var res = $http.send({ url: "https://api.upstage.ai/v1/credits", method: "GET", headers: { "Authorization": "Bearer " + key }, timeout: 20 })
  if (res.statusCode === 401) return e.json(200, { ok: false, valid: false, message: "key rejected (401) - check UPSTAGE_API_KEY" })
  if (res.statusCode === 404) return e.json(200, { ok: true, valid: true, message: "key valid - scanned-PDF OCR ready" })
  return e.json(200, { ok: res.statusCode !== 401, valid: res.statusCode !== 401, status: res.statusCode, message: "probe HTTP " + res.statusCode })
} catch (err) {
  return e.json(500, { error: String(err) })
}

}, $apis.requireSuperuserAuth())

routerAdd("GET", "/api/creator/tts-preview", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  var q = e.request.url.query()
  var model = (q.get("model") || "").trim()
  var voice = (q.get("voice") || "").trim()
  var text = (q.get("text") || "안녕하세요. 음성 미리듣기입니다.").trim().substring(0, 300)
  var key = $os.getenv("OPENROUTER_API_KEY") || ""
  if (!model) return e.json(400, { error: "model param required" })
  if (!voice) return e.json(400, { error: "voice param required" })
  if (!key) return e.json(400, { error: "OPENROUTER_API_KEY is not set in the container" })
  var res = $http.send({
    url: "https://openrouter.ai/api/v1/audio/speech",
    method: "POST", headers: { "Authorization": "Bearer " + key, "Content-Type": "application/json" },
    body: JSON.stringify({ "model": model, "input": text, "voice": voice, "response_format": "mp3" }),
    timeout: 60
  })
  if (res.statusCode !== 200) {
    return e.json(400, { error: "TTS failed (HTTP " + res.statusCode + "): " + String(res.raw || "").substring(0, 160) })
  }
  var bytes = res.body || []
  if (bytes.length < 100) return e.json(400, { error: "TTS returned empty audio" })
  return e.blob(200, "audio/mpeg", bytes)
} catch (err) {
  return e.json(500, { error: String(err) })
}

}, $apis.requireSuperuserAuth())

routerAdd("POST", "/api/creator/start", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  ensureCollections()
  var q = e.request.url.query()
  var client = null
  var isSuper = false
  if (e.auth) { try { $app.findRecordById("_superusers", e.auth.id); isSuper = true } catch (err2) {} }
  if (isSuper) {
    var cid = q.get("client") || ""
    if (!cid) return e.json(400, { error: "client query param required (id or name)" })
    try { client = $app.findRecordById("mock_clients", cid) } catch (err3) { client = null }
    if (!client) { try { client = $app.findFirstRecordByData("mock_clients", "name", cid) } catch (err4) {} }
    if (!client) return e.json(404, { error: "client not found" })
  } else {
    var keyRes = checkKey(e.request.header.get("X-API-Key"))
    if (keyRes.error) return e.json(401, { error: keyRes.error })
    client = keyRes.client
  }
  var cfg = ensureConfig(client.id)
  var count = 40
  try { count = parseInt(q.get("count") || "40", 10) } catch (err) {}
  if (isNaN(count) || count < 1 || count > 200) count = 40
  var difficulty = q.get("difficulty") || cfg.getString("difficulty_profile") || "creative+difficult"
  // Normalize teacher-portal vocabulary to pipeline profiles:
  //   creative+medium | creative+difficult | TOPIK EPIS HARD (-> hard)
  var dm = (difficulty || "").trim()
  if (dm === "TOPIK EPIS HARD") dm = "hard"
  if (["creative+medium", "creative+difficult", "hard", "standard"].indexOf(dm) === -1) dm = "creative+difficult"
  difficulty = dm
  var kind = (q.get("kind") || "full").trim()
  if (kind === "dry") kind = "dry_questions"
  if (["full","dry_questions","dry_images","dry_audio","pdf_test"].indexOf(kind) === -1) kind = "full"
  var focus = (q.get("focus") || "").trim()
  if (focus.length > 500) focus = focus.substring(0, 500)
  var parserQ = (q.get("parser") || "").trim()
  if (["auto", "local", "upstage", "mistral"].indexOf(parserQ) === -1) parserQ = ""
  var ov = {}
  if (focus) ov.focus = focus
  if (parserQ) ov.pdf_parser = parserQ
  var gen = 1
  try { gen = parseInt(q.get("gen_type") || "1", 10) } catch (err) {}
  if (isNaN(gen) || gen < 1 || gen > 3) gen = 1
  var pdfFiles = []
  try { pdfFiles = e.findUploadedFiles("pdf") } catch (err) {}
  var pdfName = ""
  if (gen >= 2 && pdfFiles.length === 0) {
    return e.json(400, { error: "PDF file required for generation type " + gen + " - upload the document as multipart field 'pdf'" })
  }
  if (pdfFiles.length > 0) {
    if (pdfFiles[0].size > 10485760) return e.json(400, { error: "PDF exceeds the 10 MB upload limit" })
    pdfName = pdfFiles[0].name || "doc.pdf"
  }
  var jobCol = $app.findCollectionByNameOrId("mock_jobs")
  var job = new Record(jobCol)
  job.set("client", client.id)
  job.set("status", "queued")
  job.set("kind", kind)
  job.set("gen_type", gen)
  job.set("count", count)
  job.set("difficulty", difficulty)
  job.set("overrides", ov)
  if (pdfFiles.length > 0) { try { job.set("pdf", pdfFiles[0]) } catch (err) {} }
  $app.save(job)
  return e.json(200, {
    job_id: job.id,
    status: "queued",
    kind: kind,
    gen_type: gen,
    client: client.getString("name"),
    count: count,
    difficulty: difficulty,
    focus: focus,
    parser: parserQ,
    pdf: pdfName
  })
} catch (err) {
  return e.json(500, { error: String(err) })
}

})

routerAdd("GET", "/api/creator/config", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  ensureCollections()
  var q = e.request.url.query()
  var cid = q.get("client") || ""
  if (!cid) return e.json(400, { error: "client query param required" })
  var cli = null
  try { cli = $app.findRecordById("mock_clients", cid) } catch (err2) { cli = null }
  if (!cli) { try { cli = $app.findFirstRecordByData("mock_clients", "name", cid) } catch (err3) {} }
  if (!cli) return e.json(404, { error: "client not found" })
  var cfg = ensureConfig(cli.id)
  return e.json(200, {
    client: cli.id,
    client_name: cli.getString("name"),
    config: cfg.id,
    record: {"name": cfg.get("name"), "llm_author_model": cfg.get("llm_author_model"), "author_provider": cfg.get("author_provider"), "gemini_model": cfg.get("gemini_model"), "dedup_enabled": cfg.get("dedup_enabled"), "dedup_sets": cfg.get("dedup_sets"), "llm_proofread_model": cfg.get("llm_proofread_model"), "llm_repair_model": cfg.get("llm_repair_model"), "image_primary": cfg.get("image_primary"), "image_fallback": cfg.get("image_fallback"), "tts_model": cfg.get("tts_model"), "tts_fallback_model": cfg.get("tts_fallback_model"), "tts_fallback_voice": cfg.get("tts_fallback_voice"), "tts_male_voice": cfg.get("tts_male_voice"), "tts_female_voice": cfg.get("tts_female_voice"), "tts_fallback_male_voice": cfg.get("tts_fallback_male_voice"), "tts_fallback_female_voice": cfg.get("tts_fallback_female_voice"), "pdf_parser": cfg.get("pdf_parser"), "upscale_pdf_images": cfg.get("upscale_pdf_images"), "question_count": cfg.get("question_count"), "reading_count": cfg.get("reading_count"), "image_count": cfg.get("image_count"), "image_count_min": cfg.get("image_count_min"), "image_count_max": cfg.get("image_count_max"), "difficulty_profile": cfg.get("difficulty_profile"), "marks_per_question": cfg.get("marks_per_question"), "max_tokens": cfg.get("max_tokens"), "temperature": cfg.get("temperature"), "timeout_s": cfg.get("timeout_s"), "retries": cfg.get("retries"), "img_retries": cfg.get("img_retries"), "push_pb_base": cfg.get("push_pb_base"), "push_pb_email": cfg.get("push_pb_email"), "push_pb_pass": cfg.get("push_pb_pass"), "push_subject_id": cfg.get("push_subject_id"), "push_exam_type": cfg.get("push_exam_type"), "push_exam_status": cfg.get("push_exam_status"), "push_enabled": cfg.get("push_enabled"), "audio_gap_ms": cfg.get("audio_gap_ms"), "sample_rate": cfg.get("sample_rate"), "tts_speed": cfg.get("tts_speed"), "tts_natural_pacing": cfg.get("tts_natural_pacing"), "tts_polish": cfg.get("tts_polish"), "tts_atempo_models": cfg.get("tts_atempo_models"), "tts_male_speed": cfg.get("tts_male_speed"), "tts_female_speed": cfg.get("tts_female_speed"), "tts_fallback_male_speed": cfg.get("tts_fallback_male_speed"), "tts_fallback_female_speed": cfg.get("tts_fallback_female_speed"), "listening_blank_count": cfg.get("listening_blank_count"), "image_grid_count": cfg.get("image_grid_count"), "image_style_prompt": cfg.get("image_style_prompt"), "audio_workers": cfg.get("audio_workers"), "prompts_json": cfg.get("prompts_json"), "active": cfg.get("active")}
  })
} catch (err) {
  return e.json(500, { error: String(err) })
}

}, $apis.requireSuperuserAuth())

routerAdd("POST", "/api/creator/delete-client", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  ensureCollections()
  var q = e.request.url.query()
  var cid = q.get("client") || ""
  if (!cid) return e.json(400, { error: "client param required" })
  var client = null
  try { client = $app.findRecordById("mock_clients", cid) } catch (err) { client = null }
  if (!client) return e.json(404, { error: "client not found" })
  var delFor = function (col) {
    var n = 0
    var recs = $app.findRecordsByFilter(col, "client = '" + cid + "'", "", 1000, 0)
    for (var i = 0; i < recs.length; i++) { try { $app.delete(recs[i]); n++ } catch (err) {} }
    return n
  }
  var deleted = {
    configs: delFor("mock_config"),
    jobs: delFor("mock_jobs"),
    dryruns: delFor("dryrun"),
    fullruns: delFor("fullrun")
  }
  $app.delete(client)
  return e.json(200, { ok: true, deleted: deleted })
} catch (err) {
  return e.json(500, { error: String(err) })
}

}, $apis.requireSuperuserAuth())

routerAdd("GET", "/api/creator/jobs", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  ensureCollections()
  var q = e.request.url.query()
  var recs = null
  var isSuper = false
  if (e.auth) { try { $app.findRecordById("_superusers", e.auth.id); isSuper = true } catch (err2) {} }
  if (isSuper) {
    var filter = "1=1"
    var cid = q.get("client") || ""
    if (cid) filter = "client = '" + cid + "'"
    var st = q.get("status") || ""
    if (st) filter = filter + " && status = '" + st + "'"
    recs = $app.findRecordsByFilter("mock_jobs", filter, "-created", 100, 0)
  } else {
    var keyRes = checkKey(e.request.header.get("X-API-Key"))
    if (keyRes.error) return e.json(401, { error: keyRes.error })
    recs = $app.findRecordsByFilter("mock_jobs", "client = '" + keyRes.client.id + "'", "-created", 50, 0)
  }
  var out = []
  for (var i = 0; i < recs.length; i++) {
    var r = recs[i]
    var cname = ""
    try { cname = $app.findRecordById("mock_clients", r.getString("client")).getString("name") } catch (err3) {}
    out.push({
      id: r.id,
      client: r.getString("client"),
      client_name: cname,
      status: r.getString("status"),
      kind: r.getString("kind") || "full",
      gen_type: r.getInt("gen_type") || 1,
      pdf: r.getString("pdf"),
      count: r.getInt("count"),
      difficulty: r.getString("difficulty"),
      focus: (function () {
        try {
          var ov = r.get("overrides")
          if (ov && typeof ov === "string") { ov = JSON.parse(ov) }
          return (ov && ov.focus) || ""
        } catch (errF) { return "" }
      })(),
      log: (r.getString("log") || "").substring(0, 300),
      pushed: r.getBool("pushed"),
      report: (function () {
        try {
          var rep = r.get("report")
          if (rep && typeof rep === "string") { rep = JSON.parse(rep) }
          return rep || null
        } catch (errR) { return null }
      })(),
      created: r.getString("created"),
      updated: r.getString("updated"),
      error: r.getString("error")
    })
  }
  return e.json(200, { jobs: out })
} catch (err) {
  return e.json(500, { error: String(err) })
}

})

routerAdd("GET", "/api/creator/jobs/{id}", (e) => {

function ensureCollections() {
  var need = ["mock_clients", "mock_config", "mock_config_meta", "mock_models", "mock_jobs", "dryrun", "fullrun"]
  var missing = []
  for (var i = 0; i < need.length; i++) {
    try { $app.findCollectionByNameOrId(need[i]) } catch (err) { missing.push(need[i]) }
  }
  if (missing.length > 0) {
    $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
  }
  if ($app.countRecords("mock_config_meta") === 0) {
    var meta = $app.findCollectionByNameOrId("mock_config_meta")
    var m0 = new Record(meta); m0.set("field", "gemini_model"); m0.set("label", "Gemini author model"); m0.set("ftype", "text"); m0.set("group", "LLM"); m0.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro"); $app.save(m0);
var m1 = new Record(meta); m1.set("field", "author_provider"); m1.set("label", "Author provider"); m1.set("ftype", "select"); m1.set("group", "LLM"); m1.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini"); m1.set("options", ["gemini", "openrouter"]); $app.save(m1);
var m2 = new Record(meta); m2.set("field", "llm_author_model"); m2.set("label", "OpenRouter fallback author"); m2.set("ftype", "text"); m2.set("group", "LLM"); m2.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded"); $app.save(m2);
var m3 = new Record(meta); m3.set("field", "llm_proofread_model"); m3.set("label", "Proofread LLM"); m3.set("ftype", "text"); m3.set("group", "LLM"); m3.set("help", "OpenRouter model for proofreading (always via OpenRouter)"); $app.save(m3);
var m4 = new Record(meta); m4.set("field", "llm_repair_model"); m4.set("label", "Repair LLM"); m4.set("ftype", "text"); m4.set("group", "LLM"); m4.set("help", "OpenRouter model for repair pass"); $app.save(m4);
var m5 = new Record(meta); m5.set("field", "dedup_enabled"); m5.set("label", "Check duplicates against previous mockups"); m5.set("ftype", "bool"); m5.set("group", "LLM"); m5.set("help", "Compares new questions against the last N mock exams in the end-user app and steers the author away from repeats"); $app.save(m5);
var m6 = new Record(meta); m6.set("field", "dedup_sets"); m6.set("label", "Check last N mockups"); m6.set("ftype", "number"); m6.set("group", "LLM"); m6.set("help", "How many of the latest mock exams to check for duplicates (default 5)"); $app.save(m6);
var m7 = new Record(meta); m7.set("field", "image_primary"); m7.set("label", "Image provider"); m7.set("ftype", "select"); m7.set("group", "Images"); m7.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)"); m7.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"]); $app.save(m7);
var m8 = new Record(meta); m8.set("field", "image_style_prompt"); m8.set("label", "Image style prompt"); m8.set("ftype", "text"); m8.set("group", "Images"); m8.set("help", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default)."); $app.save(m8);
var m9 = new Record(meta); m9.set("field", "image_fallback"); m9.set("label", "Fallback image model"); m9.set("ftype", "text"); m9.set("group", "Images"); m9.set("help", "Deprecated - providers fall back automatically, keep empty"); $app.save(m9);
var m10 = new Record(meta); m10.set("field", "image_count"); m10.set("label", "Image questions target"); m10.set("ftype", "number"); m10.set("group", "Images"); m10.set("help", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper."); $app.save(m10);
var m11 = new Record(meta); m11.set("field", "image_grid_count"); m11.set("label", "Picture grid questions"); m11.set("ftype", "number"); m11.set("group", "Images"); m11.set("help", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style."); $app.save(m11);
var m12 = new Record(meta); m12.set("field", "image_count_min"); m12.set("label", "Image questions min"); m12.set("ftype", "number"); m12.set("group", "Images"); m12.set("help", "Minimum (18)"); $app.save(m12);
var m13 = new Record(meta); m13.set("field", "image_count_max"); m13.set("label", "Image questions max"); m13.set("ftype", "number"); m13.set("group", "Images"); m13.set("help", "Maximum (26)"); $app.save(m13);
var m14 = new Record(meta); m14.set("field", "tts_model"); m14.set("label", "TTS model"); m14.set("ftype", "select"); m14.set("group", "Audio"); m14.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); m14.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); $app.save(m14);
var m15 = new Record(meta); m15.set("field", "tts_male_voice"); m15.set("label", "Male listening voice"); m15.set("ftype", "text"); m15.set("group", "Audio"); m15.set("help", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample."); $app.save(m15);
var m16 = new Record(meta); m16.set("field", "tts_female_voice"); m16.set("label", "Female listening voice"); m16.set("ftype", "text"); m16.set("group", "Audio"); m16.set("help", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample."); $app.save(m16);
var m17 = new Record(meta); m17.set("field", "tts_fallback_model"); m17.set("label", "Fallback TTS model"); m17.set("ftype", "select"); m17.set("group", "Audio"); m17.set("help", "Used only if the primary TTS model fails or times out."); m17.set("options", ["microsoft/mai-voice-2-flash", "fish-audio/s2.1-pro-free:free", "x-ai/grok-voice-tts-1.0"]); $app.save(m17);
var m18 = new Record(meta); m18.set("field", "tts_fallback_male_voice"); m18.set("label", "Fallback male listening voice"); m18.set("ftype", "text"); m18.set("group", "Audio"); m18.set("help", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m18);
var m19 = new Record(meta); m19.set("field", "tts_fallback_female_voice"); m19.set("label", "Fallback female listening voice"); m19.set("ftype", "text"); m19.set("group", "Audio"); m19.set("help", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model."); $app.save(m19);
var m20 = new Record(meta); m20.set("field", "max_tokens"); m20.set("label", "Max tokens"); m20.set("ftype", "number"); m20.set("group", "Advanced"); m20.set("help", "LLM generation cap"); $app.save(m20);
var m21 = new Record(meta); m21.set("field", "temperature"); m21.set("label", "Temperature"); m21.set("ftype", "number"); m21.set("group", "Advanced"); m21.set("help", "LLM sampling temp"); $app.save(m21);
var m22 = new Record(meta); m22.set("field", "timeout_s"); m22.set("label", "Timeout (s)"); m22.set("ftype", "number"); m22.set("group", "Advanced"); m22.set("help", "LLM call timeout"); $app.save(m22);
var m23 = new Record(meta); m23.set("field", "retries"); m23.set("label", "LLM retries"); m23.set("ftype", "number"); m23.set("group", "Advanced"); m23.set("help", "Retry count"); $app.save(m23);
var m24 = new Record(meta); m24.set("field", "img_retries"); m24.set("label", "Image retries"); m24.set("ftype", "number"); m24.set("group", "Advanced"); m24.set("help", "Per-model image retries"); $app.save(m24);
var m25 = new Record(meta); m25.set("field", "push_pb_base"); m25.set("label", "Push base URL"); m25.set("ftype", "text"); m25.set("group", "Push"); m25.set("help", "Client PocketBase base URL"); $app.save(m25);
var m26 = new Record(meta); m26.set("field", "push_pb_email"); m26.set("label", "Push email"); m26.set("ftype", "text"); m26.set("group", "Push"); m26.set("help", "Client PB admin email"); $app.save(m26);
var m27 = new Record(meta); m27.set("field", "push_pb_pass"); m27.set("label", "Push password"); m27.set("ftype", "text"); m27.set("group", "Push"); m27.set("help", "Client PB admin password"); $app.save(m27);
var m28 = new Record(meta); m28.set("field", "push_subject_id"); m28.set("label", "Subject id"); m28.set("ftype", "text"); m28.set("group", "Push"); m28.set("help", "Subject record id on client PB"); $app.save(m28);
var m29 = new Record(meta); m29.set("field", "push_exam_type"); m29.set("label", "Exam type on push"); m29.set("ftype", "text"); m29.set("group", "Push"); m29.set("help", "exam_type for the auto-created exam (mock/ubt/practice/official)"); $app.save(m29);
var m30 = new Record(meta); m30.set("field", "push_exam_status"); m30.set("label", "Exam status on push"); m30.set("ftype", "select"); m30.set("group", "Push"); m30.set("help", "draft = review-then-publish | published = immediate"); m30.set("options", ["draft", "published"]); $app.save(m30);
var m31 = new Record(meta); m31.set("field", "push_enabled"); m31.set("label", "Upload to teacher app"); m31.set("ftype", "bool"); m31.set("group", "Push"); m31.set("help", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded"); $app.save(m31);
var m32 = new Record(meta); m32.set("field", "pdf_parser"); m32.set("label", "PDF parser"); m32.set("ftype", "select"); m32.set("group", "PDF"); m32.set("help", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local."); m32.set("options", ["auto", "local", "upstage", "mistral"]); $app.save(m32);
var m33 = new Record(meta); m33.set("field", "upscale_pdf_images"); m33.set("label", "Upscale extracted paper images"); m33.set("ftype", "bool"); m33.set("group", "PDF"); m33.set("help", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used"); $app.save(m33);
var m34 = new Record(meta); m34.set("field", "audio_gap_ms"); m34.set("label", "Gap between clips (ms)"); m34.set("ftype", "number"); m34.set("group", "Audio"); m34.set("help", "Pause between sentences inside a clip. 300-500 ms sounds natural; lower feels rushed."); $app.save(m34);
var m35 = new Record(meta); m35.set("field", "sample_rate"); m35.set("label", "Sample rate (Hz)"); m35.set("ftype", "number"); m35.set("group", "Audio"); m35.set("help", "MUST match the TTS model: 44100 for fish-audio / grok-voice, 24000 for mai-voice. Wrong rate makes audio play too fast or slow."); $app.save(m35);
var m36 = new Record(meta); m36.set("field", "tts_speed"); m36.set("label", "Speech speed"); m36.set("ftype", "number"); m36.set("group", "Audio"); m36.set("help", "1.0 = normal \u00b7 0.8 = slower \u00b7 1.2 = faster \u00b7 range 0.5-2.0. Models that don't support speed ignore it."); $app.save(m36);
var m37 = new Record(meta); m37.set("field", "tts_natural_pacing"); m37.set("label", "Natural pacing"); m37.set("ftype", "bool"); m37.set("group", "Audio"); m37.set("help", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns."); $app.save(m37);
var m38 = new Record(meta); m38.set("field", "tts_polish"); m38.set("label", "Audio polish"); m38.set("ftype", "bool"); m38.set("group", "Audio"); m38.set("help", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades."); $app.save(m38);
var m39 = new Record(meta); m39.set("field", "tts_atempo_models"); m39.set("label", "Force-speed models"); m39.set("ftype", "text"); m39.set("group", "Audio"); m39.set("help", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0."); $app.save(m39);
var m40 = new Record(meta); m40.set("field", "tts_male_speed"); m40.set("label", "Male voice speed"); m40.set("ftype", "number"); m40.set("group", "Audio"); m40.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the male voice only."); $app.save(m40);
var m41 = new Record(meta); m41.set("field", "tts_female_speed"); m41.set("label", "Female voice speed"); m41.set("ftype", "number"); m41.set("group", "Audio"); m41.set("help", "0 = follow the global speech speed \u00b7 0.5-2.0 = speed for the female voice only."); $app.save(m41);
var m42 = new Record(meta); m42.set("field", "tts_fallback_male_speed"); m42.set("label", "Fallback male speed"); m42.set("ftype", "number"); m42.set("group", "Audio"); m42.set("help", "Speed for the male voice when the fallback TTS model is used (0 = follow global)."); $app.save(m42);
var m43 = new Record(meta); m43.set("field", "tts_fallback_female_speed"); m43.set("label", "Fallback female speed"); m43.set("ftype", "number"); m43.set("group", "Audio"); m43.set("help", "Speed for the female voice when the fallback TTS model is used (0 = follow global)."); $app.save(m43);
var m44 = new Record(meta); m44.set("field", "listening_blank_count"); m44.set("label", "Blank listening questions"); m44.set("ftype", "number"); m44.set("group", "Audio"); m44.set("help", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio."); $app.save(m44);
var m45 = new Record(meta); m45.set("field", "active"); m45.set("label", "Config enabled"); m45.set("ftype", "bool"); m45.set("group", "General"); m45.set("help", "Use this config"); $app.save(m45);

  // ---- migrations (idempotent, cheap after first run) ----
  try {
    var mImg = $app.findFirstRecordByData("mock_config_meta", "field", "image_primary")
    var cur = mImg.get("options") || []
    if (typeof cur === "string") { try { cur = JSON.parse(cur) } catch (errP) { cur = [] } }
    if (cur.indexOf("black-forest-labs/flux.2-klein-4b") === -1) {
      mImg.set("label", "Image provider")
      mImg.set("ftype", "select")
      mImg.set("options", ["fal-ai/z-image/turbo", "z-image", "black-forest-labs/flux.2-klein-4b"])
      mImg.set("help", "fal-ai (Fal.ai) | z-image (Magnific) | black-forest-labs/flux.2-klein-4b (OpenRouter)")
      $app.save(mImg)
    }
    var oldCfgs = $app.findRecordsByFilter("mock_config", "image_primary = 'nano-banana'", "", 200, 0)
    for (var cI = 0; cI < oldCfgs.length; cI++) {
      oldCfgs[cI].set("image_primary", "black-forest-labs/flux.2-klein-4b")
      $app.save(oldCfgs[cI])
    }
  } catch (errM) {}
  try {
    var runCols = ["dryrun", "fullrun"]
    for (var rc = 0; rc < runCols.length; rc++) {
      var col = $app.findCollectionByNameOrId(runCols[rc])
      var hasImg = false, hasAud = false
      for (var fi = 0; fi < col.fields.length; fi++) {
        var fld = col.fields[fi]
        if (fld.name === "images") hasImg = true
        if (fld.name === "audio") hasAud = true
      }
      if (!hasImg || !hasAud) {
        if (!hasImg) col.fields.add({"name": "images", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}})
        if (!hasAud) col.fields.add({"name": "audio", "type": "file", "required": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}})
        $app.save(col)
      } else {
        // upgrade single-file fields (maxSelect defaulted to 1 on old installs) to multi-file
        var changed = false
        for (var fi2 = 0; fi2 < col.fields.length; fi2++) {
          var fld2 = col.fields[fi2]
          if (fld2.name === "images" || fld2.name === "audio") {
            var opts = fld2.options || {}
            var max = Number(opts.maxSelect || 1)
            if (max < 10) { opts.maxSelect = 20; changed = true }
            if (!opts.mimeTypes || opts.mimeTypes.length === 0) {
              opts.mimeTypes = fld2.name === "images" ? ["image/webp", "image/png", "image/jpeg"] : ["audio/mpeg", "audio/mp3", "audio/wav"]
              changed = true
            }
          }
        }
        if (changed) $app.save(col)
      }
    }
  } catch (errF) {}
  try {
    var cfgCol = $app.findCollectionByNameOrId("mock_config")
    var hasVoices = false
    for (var fi3 = 0; fi3 < cfgCol.fields.length; fi3++) { if (cfgCol.fields[fi3].name === "tts_voices") hasVoices = true }
    if (!hasVoices) {
      cfgCol.fields.add({"name": "tts_voices", "type": "json", "required": false, "maxSize": 65536})
      $app.save(cfgCol)
    }
  } catch (errV) {}
  try {
    var cfgCol3 = $app.findCollectionByNameOrId("mock_config")
    var hasDup = false, hasSets = false
    for (var fi5 = 0; fi5 < cfgCol3.fields.length; fi5++) {
      var nm2 = cfgCol3.fields[fi5].name
      if (nm2 === "dedup_enabled") hasDup = true
      if (nm2 === "dedup_sets") hasSets = true
    }
    var changed3 = false
    if (!hasDup) { cfgCol3.fields.add({"name": "dedup_enabled", "type": "bool", "required": false}); changed3 = true }
    if (!hasSets) { cfgCol3.fields.add({"name": "dedup_sets", "type": "number", "required": false, "min": 0, "max": 20}); changed3 = true }
    if (changed3) $app.save(cfgCol3)
  } catch (errD) {}
  try {
    var cfgCol2 = $app.findCollectionByNameOrId("mock_config")
    var hasProv = false, hasGem = false
    for (var fi4 = 0; fi4 < cfgCol2.fields.length; fi4++) {
      var nm = cfgCol2.fields[fi4].name
      if (nm === "author_provider") hasProv = true
      if (nm === "gemini_model") hasGem = true
    }
    var changed2 = false
    if (!hasProv) { cfgCol2.fields.add({"name": "author_provider", "type": "select", "required": false, "values": ["gemini", "openrouter"], "maxSelect": 1}); changed2 = true }
    if (!hasGem) { cfgCol2.fields.add({"name": "gemini_model", "type": "text", "required": false, "max": 200}); changed2 = true }
    if (changed2) $app.save(cfgCol2)
  } catch (errG) {}
  try {
    var mProv = $app.findFirstRecordByData("mock_config_meta", "field", "author_provider")
    mProv.set("label", "Author provider")
    mProv.set("ftype", "select")
    mProv.set("options", ["gemini", "openrouter"])
    mProv.set("help", "gemini = Gemini API first (primary) with OpenRouter auto-fallback | openrouter = skip Gemini")
    mProv.set("group", "LLM")
    $app.save(mProv)
  } catch (errP) {}
  try {
    var mGem = $app.findFirstRecordByData("mock_config_meta", "field", "gemini_model")
    mGem.set("label", "Gemini author model")
    mGem.set("ftype", "text")
    mGem.set("help", "Primary author model - direct Google Gemini API (needs GEMINI_API_KEY in the container): gemini-3.6-flash, gemini-3.5-flash, gemini-3.5-flash-lite, gemini-2.5-pro")
    mGem.set("group", "LLM")
    $app.save(mGem)
  } catch (errQ) {}
  try {
    var mAuth = $app.findFirstRecordByData("mock_config_meta", "field", "llm_author_model")
    mAuth.set("label", "OpenRouter fallback author")
    mAuth.set("help", "OpenRouter model used ONLY when the Gemini author fails or quota is exceeded")
    $app.save(mAuth)
  } catch (errR) {}
  try {
    var pdfMetaDefs = [
      ["pdf_parser", "PDF parser", "select", "PDF", "Auto = PyMuPDF first, cloud OCR when scanned/garbled | Local = PyMuPDF only | Upstage = cloud OCR first | Mistral = OpenRouter vision OCR first. Selected parser runs first; on failure it falls to the next online parser, then local.", ["auto", "local", "upstage", "mistral"]],
      ["upscale_pdf_images", "Upscale extracted paper images", "bool", "PDF", "Every image extracted from the PDF is upscaled via fal-ai/recraft/upscale/crisp before upload (bills FAL credits); on failure the raw extracted image is used", null],
      ["tts_male_voice", "Male listening voice", "text", "Audio", "Voice for the male speaker (V1) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free male / MAI ko-KR-InJoon). Use the speaker icon to hear a sample.", null],
      ["tts_female_voice", "Female listening voice", "text", "Audio", "Voice for the female speaker (V2) in listening dialogues. Leave empty to auto-pick per TTS model (fish-audio free female / MAI ko-KR-Haena). Use the speaker icon to hear a sample.", null],
      ["tts_fallback_male_voice", "Fallback male listening voice", "text", "Audio", "Voice for the male speaker (V1) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["tts_fallback_female_voice", "Fallback female listening voice", "text", "Audio", "Voice for the female speaker (V2) when the run falls back to the fallback TTS model. Leave empty to auto-pick per fallback model.", null],
      ["push_enabled", "Upload to teacher app", "bool", "Push", "On = exams are uploaded to the end-user app after generation | Off = generated locally only, nothing is uploaded", null],
      ["tts_speed", "Speech speed", "number", "Audio", "1.0 = normal · 0.8 = slower · 1.2 = faster · range 0.5-2.0. Models that don't support speed ignore it.", null],
      ["tts_natural_pacing", "Natural pacing", "bool", "Audio", "Relaxed human rhythm: speed 0.92 when speed is untouched, gaps >= 450ms, extra pause after ? and ! turns.", null],
      ["tts_polish", "Audio polish", "bool", "Audio", "Post-pass on merged clips: loudness normalization + rumble filter + click-free fades.", null],
      ["tts_atempo_models", "Force-speed models", "text", "Audio", "Comma list of model fragments where speed is forced via ffmpeg atempo (pitch-preserving), e.g. x-ai/grok-voice-tts-1.0.", null],
      ["tts_male_speed", "Male voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the male voice only.", null],
      ["tts_female_speed", "Female voice speed", "number", "Audio", "0 = follow the global speech speed · 0.5-2.0 = speed for the female voice only.", null],
      ["tts_fallback_male_speed", "Fallback male speed", "number", "Audio", "Speed for the male voice when the fallback TTS model is used (0 = follow global).", null],
      ["tts_fallback_female_speed", "Fallback female speed", "number", "Audio", "Speed for the female voice when the fallback TTS model is used (0 = follow global).", null],
      ["listening_blank_count", "Blank listening questions", "number", "Audio", "Random listening questions (Q21-40) become audio-only: no question text, options are just 1/2/3/4, student marks the answer after hearing the audio.", null],
      ["image_grid_count", "Picture grid questions", "number", "Images", "How many image questions become 2x2 grid composites (one image with 4 sub-pictures, options 1/2/3/4). Flat colourful EPS-TOPIK style.", null],
      ["image_style_prompt", "Image style prompt", "text", "Images", "Style instruction wrapped around every generated image (flat colourful EPS-TOPIK vector style by default).", null],
      ["image_count", "Image questions target", "number", "Images", "How many questions carry a picture, spread randomly across reading AND listening. Applies to RANDOM generation only - Paper PDF mode follows the paper.", null]
    ]
    var mColM = $app.findCollectionByNameOrId("mock_config_meta")
    for (var pmi = 0; pmi < pdfMetaDefs.length; pmi++) {
      var pm = pdfMetaDefs[pmi]
      var mRec = null
      try { mRec = $app.findFirstRecordByData("mock_config_meta", "field", pm[0]) } catch (errM2) {}
      if (!mRec) { mRec = new Record(mColM); mRec.set("field", pm[0]) }
      mRec.set("label", pm[1])
      mRec.set("ftype", pm[2])
      mRec.set("group", pm[3])
      mRec.set("help", pm[4])
      if (pm[5]) mRec.set("options", pm[5])
      $app.save(mRec)
    }
  } catch (errS) {}
  try {
    var mT = $app.findFirstRecordByData("mock_config_meta", "field", "tts_model")
    if (mT.getString("ftype") !== "select") { mT.set("ftype", "select"); mT.set("options", ["fish-audio/s2.1-pro-free:free", "microsoft/mai-voice-2-flash", "x-ai/grok-voice-tts-1.0"]); mT.set("label", "TTS model"); mT.set("help", "Model that reads the listening scripts aloud. Options load from the models API."); $app.save(mT) }
  } catch (errT) {}
  try {
    // upsert missing default TTS model seeds (existing installs never got them)
    var mCol = $app.findCollectionByNameOrId("mock_models")
    var seedDefs = [["tts", "fish-audio/s2.1-pro-free:free", "Fish Audio S2.1 Pro (free)", "default TTS - 44100 Hz"], ["tts", "microsoft/mai-voice-2-flash", "MAI Voice 2 Flash", "fallback TTS - 24000 Hz"], ["tts", "x-ai/grok-voice-tts-1.0", "Grok Voice TTS", "alt TTS - 44100 Hz"], ["tts", "google/gemini-3.1-flash-tts-preview", "Gemini Flash TTS", "alt TTS - 24000 Hz"], ["tts", "hexgrad/kokoro-82m", "Kokoro 82M", "alt TTS - open weights"], ["tts", "mistralai/voxtral-mini-tts-2603", "Voxtral Mini TTS", "alt TTS"]]
    for (var si = 0; si < seedDefs.length; si++) {
      var exists = false
      try { exists = !!$app.findFirstRecordByData("mock_models", "model", seedDefs[si][1]) } catch (errS) {}
      if (!exists) {
        var mRec = new Record(mCol)
        mRec.set("kind", seedDefs[si][0]); mRec.set("model", seedDefs[si][1]); mRec.set("display", seedDefs[si][2]); mRec.set("notes", seedDefs[si][3])
        $app.save(mRec)
      }
    }
  } catch (errU) {}

  }
  if ($app.countRecords("mock_models") === 0) {
    var models = $app.findCollectionByNameOrId("mock_models")
    var k0 = new Record(models); k0.set("kind", "llm"); k0.set("model", "google/gemini-2.5-flash"); k0.set("display", "Gemini 2.5 Flash"); k0.set("notes", "fast author/proofread"); $app.save(k0);
var k1 = new Record(models); k1.set("kind", "llm"); k1.set("model", "google/gemini-3.5-flash"); k1.set("display", "Gemini 3.5 Flash"); k1.set("notes", "repair pass"); $app.save(k1);
var k2 = new Record(models); k2.set("kind", "llm"); k2.set("model", "qwen/qwen3.5-flash-02-23"); k2.set("display", "Qwen 3.5 Flash"); k2.set("notes", "proofread"); $app.save(k2);
var k3 = new Record(models); k3.set("kind", "llm"); k3.set("model", "claude-sonnet-4.5"); k3.set("display", "Claude Sonnet 4.5"); k3.set("notes", "alt author"); $app.save(k3);
var k4 = new Record(models); k4.set("kind", "llm"); k4.set("model", "deepseek/deepseek-v4-pro"); k4.set("display", "DeepSeek V4 Pro"); k4.set("notes", "alt repair"); $app.save(k4);
var k5 = new Record(models); k5.set("kind", "tts"); k5.set("model", "fish-audio/s2.1-pro-free:free"); k5.set("display", "Fish Audio S2.1 Pro (free)"); k5.set("notes", "default TTS - 44100 Hz"); $app.save(k5);
var k6 = new Record(models); k6.set("kind", "tts"); k6.set("model", "microsoft/mai-voice-2-flash"); k6.set("display", "MAI Voice 2 Flash"); k6.set("notes", "fallback TTS - 24000 Hz"); $app.save(k6);
var k7 = new Record(models); k7.set("kind", "tts"); k7.set("model", "x-ai/grok-voice-tts-1.0"); k7.set("display", "Grok Voice TTS"); k7.set("notes", "alt TTS - 44100 Hz"); $app.save(k7);
var k8 = new Record(models); k8.set("kind", "tts"); k8.set("model", "google/gemini-3.1-flash-tts-preview"); k8.set("display", "Gemini Flash TTS"); k8.set("notes", "alt TTS - 24000 Hz"); $app.save(k8);
var k9 = new Record(models); k9.set("kind", "tts"); k9.set("model", "hexgrad/kokoro-82m"); k9.set("display", "Kokoro 82M"); k9.set("notes", "alt TTS - open weights"); $app.save(k9);
var k10 = new Record(models); k10.set("kind", "tts"); k10.set("model", "mistralai/voxtral-mini-tts-2603"); k10.set("display", "Voxtral Mini TTS"); k10.set("notes", "alt TTS"); $app.save(k10);
var k11 = new Record(models); k11.set("kind", "image"); k11.set("model", "z-image"); k11.set("display", "z-image"); k11.set("notes", "primary image gen (5 credits/img)"); $app.save(k11);
var k12 = new Record(models); k12.set("kind", "image"); k12.set("model", "p-image-ideogram-1k"); k12.set("display", "P-Image Ideogram 1K"); k12.set("notes", "fallback (may 404)"); $app.save(k12);

  }
  try {
    // keep the mock_config / mock_jobs schemas in sync with the generator:
    // importCollections(SCHEMA, false) is idempotent and applies BOTH missing fields
    // AND changed definitions (e.g. select field values) on existing installs.
    // NOTE: fields.items() does NOT exist in the 0.39 JSVM (TypeError) - use
    // fields.getByName() for detection and importCollections to apply changes.
    var cfgCol4 = $app.findCollectionByNameOrId("mock_config")
    var needFields = ["pdf_parser", "upscale_pdf_images", "tts_male_voice", "tts_female_voice",
                      "tts_fallback_male_voice", "tts_fallback_female_voice", "tts_speed",
                      "tts_natural_pacing", "tts_polish", "tts_atempo_models",
                      "tts_male_speed", "tts_female_speed", "tts_fallback_male_speed",
                      "tts_fallback_female_speed", "listening_blank_count", "image_grid_count",
                      "image_style_prompt", "push_enabled"]
    var missingField = false
    for (var nfi = 0; nfi < needFields.length; nfi++) {
      var hasIt = false
      try { hasIt = !!cfgCol4.fields.getByName(needFields[nfi]) } catch (err) { hasIt = false }
      if (!hasIt) missingField = true
    }
    var jobsCol = $app.findCollectionByNameOrId("mock_jobs")
    var jobsNeed = ["gen_type", "pdf"]
    for (var nji = 0; nji < jobsNeed.length; nji++) {
      var hasJ = false
      try { hasJ = !!jobsCol.fields.getByName(jobsNeed[nji]) } catch (err) { hasJ = false }
      if (!hasJ) missingField = true
    }
    var pdfFieldOk = false
    try {
      var pf = cfgCol4.fields.getByName("pdf_parser")
      var pfv = (pf && ((pf.options && pf.options.values) || pf.values)) || []
      pdfFieldOk = pfv.indexOf("upstage") >= 0 && pfv.indexOf("mistral") >= 0
    } catch (errF) { pdfFieldOk = false }
    // always import when any field is missing OR the pdf_parser select values are stale
    if (missingField || !pdfFieldOk) {
      $app.importCollections([{"id": "c_mock_clients", "name": "mock_clients", "type": "base", "system": false, "fields": [{"id": "f_name", "name": "name", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_api_key_hash", "name": "api_key_hash", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config", "name": "mock_config", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_name", "name": "name", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_llm_author_model", "name": "llm_author_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_author_provider", "name": "author_provider", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["gemini", "openrouter"], "maxSelect": 1}, {"id": "f_gemini_model", "name": "gemini_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_proofread_model", "name": "llm_proofread_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_llm_repair_model", "name": "llm_repair_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_primary", "name": "image_primary", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_fallback", "name": "image_fallback", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_image_style_prompt", "name": "image_style_prompt", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_tts_model", "name": "tts_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_model", "name": "tts_fallback_model", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_voice", "name": "tts_fallback_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_voices", "name": "tts_voices", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_tts_male_voice", "name": "tts_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_female_voice", "name": "tts_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_male_voice", "name": "tts_fallback_male_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_tts_fallback_female_voice", "name": "tts_fallback_female_voice", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_pdf_parser", "name": "pdf_parser", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["auto", "local", "upstage", "mistral"], "maxSelect": 1}, {"id": "f_upscale_pdf_images", "name": "upscale_pdf_images", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_question_count", "name": "question_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_reading_count", "name": "reading_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 50}, {"id": "f_image_count", "name": "image_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_min", "name": "image_count_min", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_count_max", "name": "image_count_max", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 40}, {"id": "f_image_grid_count", "name": "image_grid_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 15}, {"id": "f_difficulty_profile", "name": "difficulty_profile", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard"], "maxSelect": 1}, {"id": "f_marks_per_question", "name": "marks_per_question", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 20}, {"id": "f_max_tokens", "name": "max_tokens", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 100000}, {"id": "f_temperature", "name": "temperature", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2}, {"id": "f_timeout_s", "name": "timeout_s", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 10, "max": 3600}, {"id": "f_retries", "name": "retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_img_retries", "name": "img_retries", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_push_pb_base", "name": "push_pb_base", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 300}, {"id": "f_push_pb_email", "name": "push_pb_email", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_pb_pass", "name": "push_pb_pass", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_push_subject_id", "name": "push_subject_id", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_push_exam_type", "name": "push_exam_type", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_exam_status", "name": "push_exam_status", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 20}, {"id": "f_push_enabled", "name": "push_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_enabled", "name": "dedup_enabled", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_dedup_sets", "name": "dedup_sets", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 20}, {"id": "f_audio_gap_ms", "name": "audio_gap_ms", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 5000}, {"id": "f_sample_rate", "name": "sample_rate", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 8000, "max": 48000}, {"id": "f_tts_speed", "name": "tts_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0.5, "max": 2.0}, {"id": "f_tts_natural_pacing", "name": "tts_natural_pacing", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_polish", "name": "tts_polish", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_tts_atempo_models", "name": "tts_atempo_models", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 500}, {"id": "f_tts_male_speed", "name": "tts_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_female_speed", "name": "tts_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_male_speed", "name": "tts_fallback_male_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_tts_fallback_female_speed", "name": "tts_fallback_female_speed", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 2.0}, {"id": "f_listening_blank_count", "name": "listening_blank_count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 10}, {"id": "f_audio_workers", "name": "audio_workers", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 16}, {"id": "f_prompts_json", "name": "prompts_json", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_active", "name": "active", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_config_meta", "name": "mock_config_meta", "type": "base", "system": false, "fields": [{"id": "f_field", "name": "field", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_label", "name": "label", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_ftype", "name": "ftype", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["text", "number", "select", "bool", "json"], "maxSelect": 1}, {"id": "f_options", "name": "options", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_group", "name": "group", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_help", "name": "help", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_order", "name": "order", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 0, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_models", "name": "mock_models", "type": "base", "system": false, "fields": [{"id": "f_kind", "name": "kind", "type": "select", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "values": ["llm", "tts", "image"], "maxSelect": 1}, {"id": "f_model", "name": "model", "type": "text", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_display", "name": "display", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 200}, {"id": "f_notes", "name": "notes", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 1000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_mock_jobs", "name": "mock_jobs", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": true, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["queued", "running", "done", "failed"], "maxSelect": 1}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["full", "dry_questions", "dry_images", "dry_audio", "pdf_test"], "maxSelect": 1}, {"id": "f_gen_type", "name": "gen_type", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 3}, {"id": "f_pdf", "name": "pdf", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 1, "maxSize": 10485760, "mimeTypes": ["application/pdf", "application/x-pdf"]}}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["creative+difficult", "creative+medium", "hard", ""], "maxSelect": 1}, {"id": "f_overrides", "name": "overrides", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 65536}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_pushed", "name": "pushed", "type": "bool", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_error", "name": "error", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 2000}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_dryrun", "name": "dryrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["dry_questions", "dry_images", "dry_audio"], "maxSelect": 1}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 10, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}, {"id": "c_fullrun", "name": "fullrun", "type": "base", "system": false, "fields": [{"id": "f_client", "name": "client", "type": "relation", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "collectionId": "c_mock_clients", "maxSelect": 1, "minSelect": 0}, {"id": "f_kind", "name": "kind", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 50}, {"id": "f_status", "name": "status", "type": "select", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "values": ["done", "failed"], "maxSelect": 1}, {"id": "f_count", "name": "count", "type": "number", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "min": 1, "max": 200}, {"id": "f_difficulty", "name": "difficulty", "type": "text", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "max": 100}, {"id": "f_questions", "name": "questions", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_report", "name": "report", "type": "json", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "maxSize": 2097152}, {"id": "f_log", "name": "log", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_images", "name": "images", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["image/webp", "image/png", "image/jpeg"]}}, {"id": "f_audio", "name": "audio", "type": "file", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "options": {"maxSelect": 20, "maxSize": 20971520, "mimeTypes": ["audio/mpeg", "audio/mp3", "audio/wav"]}}, {"id": "f_summary", "name": "summary", "type": "editor", "required": false, "presentable": false, "hidden": false, "primaryKey": false}, {"id": "f_created", "name": "created", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true}, {"id": "f_updated", "name": "updated", "type": "autodate", "required": false, "presentable": false, "hidden": false, "primaryKey": false, "onCreate": true, "onUpdate": true}], "indexes": [], "listRule": null, "viewRule": null, "createRule": null, "updateRule": null, "deleteRule": null, "options": {}}], false)
    }
  } catch (errH) { try { $app.logger().info("mig-cfg4: " + String(errH)) } catch (errL) {} }
}

function checkKey(apiKey) {
  if (!apiKey || apiKey.length === 0) return { error: "missing X-API-Key header" }
  var hash = $security.sha256(apiKey)
  var client = null
  try { client = $app.findFirstRecordByData("mock_clients", "api_key_hash", hash) } catch (err) {}
  if (!client) return { error: "invalid API key" }
  if (!client.getBool("active")) return { error: "client is disabled" }
  return { client: client }
}

function ensureConfig(clientId) {
  var cfg = null
  try { cfg = $app.findFirstRecordByData("mock_config", "client", clientId) } catch (err) {}
  if (!cfg) {
    var col = $app.findCollectionByNameOrId("mock_config")
    cfg = new Record(col)
    cfg.set("client", clientId)
    cfg.set("name", "default"); cfg.set("llm_author_model", "google/gemini-2.5-flash"); cfg.set("author_provider", "gemini"); cfg.set("gemini_model", "gemini-3.5-flash"); cfg.set("dedup_enabled", true); cfg.set("dedup_sets", 5); cfg.set("llm_proofread_model", "qwen/qwen3.5-flash-02-23"); cfg.set("llm_repair_model", "google/gemini-2.5-flash"); cfg.set("image_primary", "z-image"); cfg.set("image_fallback", "p-image-ideogram-1k"); cfg.set("tts_model", "fish-audio/s2.1-pro-free:free"); cfg.set("tts_fallback_model", "microsoft/mai-voice-2-flash"); cfg.set("tts_fallback_voice", "ko-KR-Haena:MAI-Voice-2"); cfg.set("tts_male_voice", ""); cfg.set("tts_female_voice", ""); cfg.set("tts_fallback_male_voice", ""); cfg.set("tts_fallback_female_voice", ""); cfg.set("pdf_parser", "auto"); cfg.set("upscale_pdf_images", true); cfg.set("question_count", 40); cfg.set("reading_count", 20); cfg.set("image_count", 22); cfg.set("image_count_min", 18); cfg.set("image_count_max", 26); cfg.set("difficulty_profile", "creative+medium"); cfg.set("marks_per_question", 1); cfg.set("max_tokens", 32000); cfg.set("temperature", 0.7); cfg.set("timeout_s", 600); cfg.set("retries", 3); cfg.set("img_retries", 4); cfg.set("push_pb_base", "https://ubt.wts.com.np"); cfg.set("push_pb_email", "shiva@cld.com.np"); cfg.set("push_pb_pass", ""); cfg.set("push_subject_id", "illfosglou0e3j6"); cfg.set("push_exam_type", "mock"); cfg.set("push_exam_status", "draft"); cfg.set("push_enabled", true); cfg.set("audio_gap_ms", 300); cfg.set("sample_rate", 44100); cfg.set("tts_speed", 1.0); cfg.set("tts_natural_pacing", false); cfg.set("tts_polish", false); cfg.set("tts_atempo_models", "x-ai/grok-voice-tts-1.0"); cfg.set("tts_male_speed", 0.0); cfg.set("tts_female_speed", 0.0); cfg.set("tts_fallback_male_speed", 0.0); cfg.set("tts_fallback_female_speed", 0.0); cfg.set("listening_blank_count", 5); cfg.set("image_grid_count", 6); cfg.set("image_style_prompt", "Simple flat vector illustration in the standard Korean EPS-TOPIK test style, VIVID COLOURFUL palette (never black-and-white, never muted), minimal clean line art, plain solid white background, simple everyday scene, centered single main subject, clear silhouette, no gradients, no photorealism, no text, no letters, no numbers, no watermark, no border"); cfg.set("audio_workers", 4); cfg.set("prompts_json", {}); cfg.set("active", true); 
    $app.save(cfg)
  }
  return cfg
}

try {
  ensureCollections()
  var isSuper = false
  if (e.auth) { try { $app.findRecordById("_superusers", e.auth.id); isSuper = true } catch (err2) {} }
  var client = null
  if (!isSuper) {
    var keyRes = checkKey(e.request.header.get("X-API-Key"))
    if (keyRes.error) return e.json(401, { error: keyRes.error })
    client = keyRes.client
  }
  var id = e.request.pathValue("id")
  var rec = null
  try { rec = $app.findRecordById("mock_jobs", id) } catch (err) {}
  if (!rec) return e.json(404, { error: "job not found" })
  if (!isSuper && rec.getString("client") !== client.id) return e.json(403, { error: "forbidden" })
  var cname = ""
  try { cname = $app.findRecordById("mock_clients", rec.getString("client")).getString("name") } catch (err3) {}
  return e.json(200, {
    id: rec.id,
    client: rec.getString("client"),
    client_name: cname,
    status: rec.getString("status"),
    kind: rec.getString("kind") || "full",
    gen_type: rec.getInt("gen_type") || 1,
    pdf: rec.getString("pdf"),
    count: rec.getInt("count"),
    difficulty: rec.getString("difficulty"),
    overrides: rec.get("overrides"),
    log: rec.getString("log"),
    report: rec.get("report"),
    error: rec.getString("error"),
    pushed: rec.getBool("pushed"),
    created: rec.getString("created"),
    updated: rec.getString("updated")
  })
} catch (err) {
  return e.json(500, { error: String(err) })
}

})
